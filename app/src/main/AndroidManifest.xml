<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.chatsapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/chatsapplogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/chatsapplogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatsApp"
        tools:replace="android:allowBackup,android:theme,android:icon,android:roundIcon">
        <activity
            android:name=".ui.SecondActivity"
            android:exported="true" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>